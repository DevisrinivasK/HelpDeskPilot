# Base image
FROM node:20-alpine

# Set working directory
WORKDIR /app

# Copy package files (to be added later)
# COPY package*.json ./

# Install dependencies (to be run later)
# RUN npm install

# Copy source code (to be added later)
# COPY . .

# Expose port
EXPOSE 8080

# Command to run the app (placeholder)
CMD ["echo", "Backend not yet implemented"]